bit = 00000000000000000000000000000001

function flippingBits(n) {
    //~ is the bitwise NOT operator and is used to invert bits (0 -> 1) 
    //>>> returns it as an unsigned integer 
    console.log(~n >> 0)
}

flippingBits(bit)